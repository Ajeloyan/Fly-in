[project]
name = "flyin"
version = "0.1.0"
description = "Drone routing and simulation system for the 42 Fly-in project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "arcade>=3.3.3",
]

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "mypy>=2.3.0",
    "pytest>=9.1.1",
]
